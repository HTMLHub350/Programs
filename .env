# Local development environment variables
# Do NOT commit real secrets to this file in production.

# Port used by the simple static server (see package.json start script)
PORT=8080

# Public URL (useful if deploying behind a base path)
PUBLIC_URL=/

# App mode
NODE_ENV=development
